# Benchmarks Moved

The older synthesis benchmark notebook and method-comparison experiment were
moved to the separate sibling evaluation repo:

- `/Users/maxghenis/CosilicoAI/microplex-evals/reviews/us-method-benchmark-2026-03.md`

That workspace now owns the clean rewrite of the QRF / ZI-QRF / QDNN /
ZI-QDNN / MAF-family holdout-coverage benchmark, its artifacts, and the review
handoff documentation.

`microplex` keeps shared runtime code and benchmark math. Evaluation
orchestration, experiment matrices, and papers now live in `microplex-evals`.
